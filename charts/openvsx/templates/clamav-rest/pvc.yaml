{{- if .Values.clamav.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clamav-db
  annotations:
    "helm.sh/resource-policy": keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
{{- end }}
